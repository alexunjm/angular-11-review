import { SumDetailPipe } from './sum-detail.pipe';

describe('SumDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new SumDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
