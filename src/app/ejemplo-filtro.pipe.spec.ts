import { EjemploFiltroPipe } from './ejemplo-filtro.pipe';

describe('EjemploFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new EjemploFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
